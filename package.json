{
  "name": "aniko",
  "version": "2.0.0",
  "description": "AniKo — Cebu's farm-to-city digital produce marketplace (Supabase backend)",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "install:all": "npm install --prefix frontend"
  }
}
